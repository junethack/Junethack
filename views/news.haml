- news = News.where("created_at <= ?", DateTime.now).order(created_at: :desc)
- if news.size > 0
  =h2 'News and Announcements'
  - for n in news
    %article= "<b>#{n.created_at.strftime("%Y-%m-%d")}</b>: #{n.html}"
